I want you to update my React app GridScopeNY by porting specific analytical functionality from my legacy Streamlit app.

Important:
Use my legacy app as a reference implementation for logic and workflow, but do NOT blindly copy the Streamlit code.
Rebuild the features cleanly for the current React architecture and current GridScopeNY design system.

Reference file:
- legacy_app.py

Legacy reference points to inspect:
- ttcf_scrape(...)
- oic_scrape(...)
- find_clean_prints(...)
- build_congestion_pivot_and_stacked_chart(...)
- render_constraint_impact_tool(...)
- the legacy generation section where stacked fuel-mix bars and OIC data are displayed

Goal:
Bring over the missing market-analysis workflows from the legacy app and integrate them into the modern React product.

I need the following changes:

1. Add OIC commitment data to the Generation dashboard
Use the legacy app’s OIC logic as the reference.

What I want:
- Pull and display Operator-Initiated Commitments (OIC) data on the Generation page
- Add a clean OIC section below or alongside the main generation visuals
- Include:
  - selected date control
  - OIC table
  - empty state message if no OIC data exists
- If useful, add summary cards like:
  - OIC events count
  - commitment types count
  - earliest/latest event time
- Keep it integrated with the Generation page, not as a random raw table dump

Reference:
- use the legacy oic_scrape logic and the way the legacy generation tab displays OIC data

2. Make the Constraint Impact Analysis tool match the legacy app workflow
I want the current/new Constraint Impact Analysis feature in the Congestion tab to be structured like the one in the legacy app.

What I want:
- Use render_constraint_impact_tool from the legacy app as the conceptual blueprint
- Preserve the analytical workflow:
  - market toggle (DA / RT)
  - monitored element selection
  - contingency selection
  - timestamp / HE selection
  - clean print / isolated print logic if feasible
  - zonal impact section
  - generator impact section
- Use find_clean_prints logic from the legacy app as reference for identifying isolated prints
- Rebuild it cleanly in React with modern GridScopeNY UI components
- Keep it visually polished and product-like

Important:
Do not invent a new workflow from scratch.
I want the new tool to preserve the spirit and structure of the old one, but implemented properly in the current app.

3. Change the congestion analysis line graph to a stacked bar graph
The current main congestion time-series visual should be changed from a line graph to a stacked bar chart.

What I want:
- Use build_congestion_pivot_and_stacked_chart from the legacy app as the reference
- The chart should stack constraint costs by monitored element / contingency across HE or time
- Keep it readable and consistent with the new UI
- If needed, add filtering for top constraints to avoid unreadable charts
- Preserve the ability to understand which constraints contributed to total congestion cost

4. Change the Generation fuel mix line graph to a stacked bar chart
The main fuel-mix chart on the Generation page should become a stacked bar chart instead of a line graph.

What I want:
- Use the legacy generation tab’s stacked-bar logic as the reference
- Prefer an OPA/day-level stacked bar if that is what best matches the old analytical intent, but adapt sensibly to the current app and available data
- Keep the chart clean and readable
- Preserve fuel-type breakdown clearly
- This should feel like a proper fuel-mix composition chart, not just a style swap

5. Add TTCF derates table into Interface Flows
I want the Interface Flows tab to include TTCF / derate context based on the legacy app.

What I want:
- Use ttcf_scrape from the legacy app as the reference for pulling and cleaning TTCF data
- Add the TTCF derates table to the Interface Flows page
- Include:
  - date selector
  - interface group filter:
    - All
    - External
    - Internal
  - interface multiselect
- Use the same cleaned/remapped interface names concept from the legacy app
- Show the filtered TTCF derates table in a polished card/section
- Integrate it naturally with the interface flows page rather than making it feel like a random appended table

6. Preserve and reuse legacy interface name remapping
The TTCF data cleaning in the legacy app includes explicit interface-name remapping.

Please:
- inspect the mapping logic in ttcf_scrape
- preserve or adapt that mapping for the current app
- make sure interface names used in TTCF context align with names used in the Interface Flows page
- avoid raw operator naming in the UI where a cleaner display name is available

7. Keep the current React product design
Important:
- keep the recent polished GridScopeNY UI direction
- use modern cards, filters, chart containers, and tables
- do not revert to a raw admin-tool feel
- port the old analysis logic, not the old presentation layer

8. Data handling / architecture
Please inspect:
- whether the current app already has the needed datasets loaded
- whether any additional preprocessing/parquet outputs are needed
- whether the old logic should be turned into reusable backend selectors or utility functions
- whether joins/filters/time handling need cleanup before porting these features

9. Deliverables
Please:
1. inspect the legacy_app.py code relevant to these features
2. explain what logic you are carrying over from the legacy app
3. implement the changes in the current React app
4. list what files changed
5. explain any assumptions or adaptations made because of differences between the old and new app

Acceptance criteria:
- Generation page includes OIC commitment data using the legacy app’s workflow as reference
- Constraint Impact Analysis is structured like the legacy tool
- Congestion’s main chart is a stacked bar chart
- Generation fuel-mix chart is a stacked bar chart
- Interface Flows includes a TTCF derates table with date/group/interface filtering
- Interface naming is remapped/cleaned consistently
- The new app keeps its modern product feel