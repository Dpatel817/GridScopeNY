I only want to add NYISO Interconnection Queue functionality to my project. Do not change anything else unless required for integration.

Project context:
- React/TypeScript app
- Existing frontend already uses dataset hooks like:
  - useDataset(...)
  - useInventory(...)
- Home page and data architecture already exist
- I want this added cleanly into the existing dataset pipeline and UI

Goal:
Add a new NYISO Interconnection Queue dataset by scraping and processing this Excel file:
https://www.nyiso.com/documents/20142/1407078/NYISO-Interconnection-Queue.xlsx

I also want the system to detect whether there are new additions or changes compared with the previous scrape.

What I need implemented

1. Scrape + parse the workbook
Create logic that:
- downloads the Excel file from the NYISO URL
- parses the workbook robustly
- handles multiple sheets
- normalizes the data into a clean internal format
- stores the processed output in the same dataset format/style my app already uses

The workbook may contain sheets like:
- Interconnection Queue
- Cluster Projects
- Affected System Studies
- Withdrawn
- Cluster Projects-Withdrawn
- Affected System- Withdrawn
- In Service

Be defensive about sheet names and column naming inconsistencies.

2. Create processed datasets
At minimum, create:
- interconnection_queue_active
- interconnection_queue_cluster_projects
- interconnection_queue_affected_system
- interconnection_queue_withdrawn
- interconnection_queue_in_service
- interconnection_queue_summary
- interconnection_queue_changes

If the architecture prefers a single normalized dataset plus derived summary/changes datasets, that is fine too.

3. Normalize useful columns
Please extract and standardize fields where available, such as:
- queue_id
- queue_pos
- project_name
- developer
- fuel_type
- county
- state
- zone
- utility
- point_of_interconnection
- status
- sp_mw
- wp_mw
- storage_capability
- discharge_duration
- proposed_cod
- proposed_sync_date
- proposed_in_service_date
- date_of_ir
- ia_tender_date
- fs_complete_date
- source_sheet
- scrape_timestamp

4. Add snapshot comparison / change detection
I want to know if something is new or changed since the prior scrape.

Implement snapshot tracking so the system:
- saves the current normalized snapshot
- compares it to the most recent previous snapshot
- detects:
  - new projects
  - removed projects
  - updated projects

Use a stable key in this order:
1. queue_id or queue_pos if available
2. otherwise fallback to a composite key such as project_name + developer + source_sheet

Create a changes dataset with fields like:
- change_type (new / removed / updated)
- queue_id
- queue_pos
- project_name
- developer
- source_sheet
- changed_fields
- previous_values
- current_values
- detected_at

Also create a summary dataset that includes:
- total_active_projects
- total_cluster_projects
- total_withdrawn
- total_in_service
- new_since_last_scrape
- removed_since_last_scrape
- updated_since_last_scrape
- latest_scrape_timestamp

5. Frontend integration
Add the interconnection queue into the existing frontend in a minimal clean way.

Please:
- inspect how datasets are registered and exposed to useDataset/useInventory
- wire in the new interconnection queue datasets
- make sure they appear as available in inventory
- add a simple UI view for the interconnection queue data

Preferred UI:
- a compact table or card section
- summary metrics at the top
- recent changes list
- badge/indicator if new additions exist since last scrape

Keep the UI simple and consistent with the existing app style.

6. Error handling
- if scrape/download fails, do not break the app
- preserve prior snapshot if it exists
- expose a graceful error or stale-data state
- log useful debug info

7. Deliverables
Please do all of the following:
1. Inspect the codebase and identify exactly which files need changes
2. Implement the feature
3. Show me:
   - files changed
   - new files added
   - key code snippets
   - any commands I need to run
4. Make reasonable assumptions and proceed without blocking

Important constraints:
- Focus only on the interconnection queue feature
- Do not refactor unrelated parts of the app
- Keep code modular and TypeScript-friendly
- Reuse existing dataset patterns wherever possible