I need you to update the Congestion Analysis page in my React/TypeScript NYISO dashboard.

Important:
- Focus only on the Congestion page and the components/hooks/utilities it depends on.
- Do not refactor unrelated pages unless required for shared controls or reusable chart components.
- Keep the same overall product/UI direction we’ve been applying to the other analytics pages (Price, Demand, Generation, Interface Flows), but with the congestion-specific adjustments below.
- First inspect the codebase and identify the exact files responsible for:
  - the Congestion page
  - KPI cards
  - congestion summary box
  - chart rendering
  - resolution controls
  - constraint selectors
  - dataset fetching via useDataset/useInventory
  - binding constraints table
  - zonal impact section
  - generator-level impact section
  - duplicated stacked bar chart section
- Then implement the edits below end-to-end.

GOALS

I want the Congestion page to follow the same upgraded analytics-page style as the other pages, with these specific congestion adjustments:
1. Move/enhance the congestion summary to the top.
2. Replace the top control bar with a side chart control panel.
3. Apply the same richer charting model as the other pages.
4. Eliminate the separate duplicated stacked bar chart section.
5. Make the Binding Constraints table a toggle expander instead of always fully open.
6. Make the Zonal Impact and Generator-Level Impact sections expandable/collapsible instead of always showing the top X rows immediately.
7. Preserve the constraint impact analysis functionality, but make the page much less vertically overwhelming.
8. Keep the page clean, modern, and consistent with the rest of the dashboard.

DETAILS / REQUIRED BEHAVIOR

PART 1 — Move Congestion Summary to the top
Move the congestion/constraint summary box to the top of the page, above KPI cards and charts.

Enhance the summary:
- If the app already has OpenAI/API-based summary generation, reuse it.
- If no AI integration exists, create a clean fallback architecture but do not block the page.
- The summary must be grounded in computed congestion statistics only.
- It should produce concise commentary on things like:
  - total congestion cost
  - highest-cost binding constraint
  - binding count / concentration
  - whether congestion was broad-based or concentrated in a small number of constraints
  - notable positive vs negative constraint cost behavior if relevant
  - whether a small number of constraints drove most of the impact
- Include a deterministic fallback summary if AI is unavailable or fails.

Suggested helper structure:
- buildCongestionSummaryContext(...)
- generateCongestionSummary(...)

PART 2 — KPI cards should use fixed congestion insight metrics
Replace the current KPI logic.

The KPI cards should always show fixed congestion intelligence metrics, regardless of selected chart resolution.

Required KPI metrics:
1. On-Peak Total Constraint Cost
2. On-Peak Avg Constraint Cost
3. Peak Positive Constraint Cost + constraint + hour/date
4. Peak Negative Constraint Cost + constraint + hour/date
5. Highest-Cost Binding Constraint
6. Avg Cost of Top Constraint
7. Binding Constraints Count
8. Concentration Metric (for example top 3 share of total cost, or another defensible concentration measure)

Definitions / guidance:
- On-Peak should use HE 7–22 unless the project already uses a different standard
- Constraint cost metrics should be computed from the base congestion data, not from transformed chart views
- If negative congestion values are meaningful in the source data, preserve that logic
- Concentration Metric should be something intuitive like:
  - “Top 3 constraints drove X% of total cost”

Important:
- These KPI values must come from the base source datasets, not from whatever transformed chart view is currently selected
- Do not tie KPI card values to the chart resolution selector

UI guidance:
- Keep KPI styling aligned with current dashboard cards
- Use a responsive two-row grid if needed
- Prioritize readability over squeezing too much into one row

PART 3 — Replace the top control bar with side “Chart Controls”
The current resolution / constraint selector bar above the charts should be removed and replaced with a side control panel associated with the main congestion chart.

Create a reusable controls component, for example:
- CongestionChartControls.tsx

The control panel should include:
1. Constraint selector
   - multi-select
   - “All Constraints” shortcut
   - default to a sensible subset if showing all creates too much clutter
2. Resolution / aggregation selector
   - Hourly
   - On-Peak Average
   - Off-Peak Average
   - Daily Average
3. Date range selector
   - Today (default)
   - Custom start/end date
   - All Dates
4. Chart type selector
   - line with markers
   - regular line chart
   - stacked area chart
   - DO NOT include stacked bar chart
5. Optional useful toggles if appropriate:
   - show legend
   - smooth lines on/off
   - show top N only
   - sort series by cost contribution
   - normalize off by default

Important:
- Remove/eliminate the separate stacked bar chart section that currently duplicates the charting experience.
- If stacked area is not sensible in some sign-mixed configurations, disable it gracefully with a short note.

Layout preference:
- desktop: controls on the side of the main chart card
- smaller screens: controls collapse above or below the chart
- keep the implementation polished and consistent with the rest of the app

PART 4 — Main congestion chart behavior
Update the main “Constraint Costs Over Time” chart so it supports the new controls.

Required behavior:
- user can select one or more constraints
- user can choose resolution:
  - Hourly
  - OPA
  - OFFPA
  - Daily average
- user can choose date range:
  - today by default
  - custom date range
  - all dates
- user can switch chart type between:
  - line with markers
  - line
  - stacked area
- DO NOT offer stacked bar chart

Important:
- eliminate the separate duplicated stacked bar chart module lower on the page
- the main chart should become the primary charting surface for congestion
- if stacked area is misleading for a given sign-mixed selection, either:
  - implement a safe version, or
  - disable it with a helpful explanatory note
- preserve positive/negative sign semantics correctly

Tooltip / hover behavior:
- hovering a timestamp should display all visible constraint values clearly at that timestamp
- tooltip should show:
  - timestamp/date
  - constraint name
  - value in $
- preserve sign clearly
- markers mode should make hovered points easy to see

PART 5 — Binding Constraints table should be an expander
The “All Binding Constraints” table currently makes the page too long by always rendering fully open.

Change it so:
- it becomes a toggle expander / collapsible section
- collapsed by default, unless there is a strong existing reason otherwise
- header should clearly show something like:
  - “Binding Constraints (28)”
- when expanded, show the full table
- optionally allow a compact preview row count or a “top 5 preview” above expansion if easy, but not required

Requirements:
- preserve the useful columns already present
- preserve sorting logic if it exists
- do not remove useful data
- the goal is to reduce vertical sprawl, not remove content

PART 6 — Remove the duplicated stacked bar chart section
There is a duplicated congestion stacked bar chart section lower on the page that should be eliminated.

Requirements:
- remove that separate chart section entirely
- do not leave dead controls behind
- consolidate charting into the main congestion chart experience
- if any useful functionality from that section exists, fold it into the main chart controls rather than keeping a second chart block

PART 7 — Make Zonal Impact and Generator-Level Impact sections expandable
In the Constraint Impact Analysis area, make the Zonal Impact and Generator-Level Impact sections expandable/collapsible.

What I want:
- do not immediately show the full “top X zones” and “top X generators” lists by default
- instead, render each as a clean expander section
- examples:
  - “Zonal Impact Analysis”
  - “Generator-Level Impact Analysis”
- collapsed by default is preferred unless there is a good reason not to
- when expanded, show the full table/content
- remove or reduce the visual emphasis on “top X zones” as the main framing
- keep the data available, just don’t force it all onto the page at once

Requirements:
- preserve the current analysis functionality
- preserve filters/selectors above the impact analysis
- keep the interpretation summary if it exists, but place it cleanly within or just above the related expander
- avoid making users scroll past large tables unless they choose to expand them

Optional enhancement if easy:
- allow the expander header to show a short summary snippet like:
  - “15 zones ranked by |MCC|”
  - “25 generators ranked by |MCC|”

PART 8 — Constraint Impact Analysis should stay, but feel lighter
Keep the Constraint Impact Analysis section, including:
- market toggle
- date selector
- monitored element selector
- contingency selector
- hour selector
- KPIs for selected print
- zonal impact data
- generator-level impact data

But improve hierarchy so it feels modular and less overwhelming:
- selection controls at top of the section
- print-specific KPI cards beneath
- then expandable Zonal Impact and Generator-Level Impact subsections
- keep interpretation summary aligned with the zonal section if it still makes sense

Do not remove functionality here.
Just improve the presentation and information density.

PART 9 — Data transformation requirements
Please inspect the current congestion dataset structure and create clean reusable transformation utilities.

I want reusable helpers for:
- filtering by date range
- filtering by selected constraints
- computing OPA
- computing OFFPA
- computing daily averages
- computing total congestion cost
- computing positive and negative cost extremes
- computing binding counts
- computing top-constraint contribution / concentration
- computing KPI stats
- building AI summary context

Suggested utility files if needed:
- congestionTransforms.ts
- congestionMetrics.ts
- congestionSummary.ts
- congestionChartOptions.ts

Be careful with:
- timestamp parsing
- constraint naming consistency
- missing/null values
- positive vs negative cost semantics
- timezone assumptions
- HE logic for OPA/OFFPA
- concentration calculations
- preserving the existing constraint-impact analysis logic

PART 10 — Design / UX expectations
Keep the page visually close to the existing dashboard style:
- clean card layout
- rounded corners
- soft borders
- readable typography
- modern analytics feel

Desired hierarchy:
1. Congestion Summary at the top
2. KPI cards below summary
3. Main congestion chart section with side Chart Controls
4. Binding Constraints section as an expander
5. Constraint Impact Analysis section
6. Within that, make Zonal Impact and Generator-Level Impact each expanders
7. Remove the separate duplicated stacked bar chart section entirely

Avoid clutter.
Do not make the page feel like a raw developer dashboard.
Do not force the user to scroll through giant always-open tables.

PART 11 — Shared-component reuse
If the Price, Demand, Generation, and Interface Flows pages already have or will have reusable chart controls/components after recent edits, reuse them where sensible rather than duplicating logic.

For example:
- shared side control panel
- shared date range picker
- shared multi-select control
- shared chart wrapper/card component
- shared tooltip formatter
- shared summary card pattern
- shared expander/collapsible section pattern

But do not over-refactor unrelated modules unless needed.

PART 12 — Deliverables I want from you
After implementing, show me:
1. Which files you changed
2. Which new files you added
3. The key code snippets
4. Any commands I need to run
5. Any assumptions you made

Please do not stop to ask me follow-up questions unless absolutely necessary.
Make reasonable assumptions from the current codebase and implement the cleanest working version.