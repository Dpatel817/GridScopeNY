I want you to improve interface handling in GridScopeNY by explicitly classifying interfaces as internal vs external and remapping / normalizing interface names.

Goal:
Make the Interface Flows page more intuitive and trustworthy by cleaning up naming and categorization.

Requirements:

1. Add interface classification
For each interface, classify it as one of:
- Internal
- External
If useful, allow additional labels such as:
- NYISO internal
- External import/export path
- scheduled path
Use the cleanest classification supported by the data.

2. Normalize interface names
There are multiple raw interface names in the data that are not user-friendly.
I want a cleaner naming layer.

Please:
- audit current interface names in the dataset
- define a normalized display name for each
- keep the raw name in the data if needed
- use the cleaner display name in the UI

Examples of what I want conceptually:
- cleaner path labels
- consistent use of SCH / path names
- clearer naming for external paths
- better readability for users who are not looking at raw operator-style naming

3. Interface metadata layer
Create a reusable interface metadata mapping that includes:
- raw name
- display name
- internal/external class
- optional directionality notes
- optional region/path family

4. UI improvements
Update the Interface Flows page so users can:
- filter by internal vs external
- filter by normalized display name
- see classification clearly in tables and charts
- optionally group interfaces by class

5. Product usefulness
This should help the user understand:
- which interfaces are internal transfer paths
- which interfaces are external import/export paths
- which paths are most active / stressed
- how the interface names map to something readable

6. Data handling
Do not break the original raw data.
Use a clean mapping/enrichment layer.

7. Output
Please:
- implement interface classification and name remapping
- explain the mapping logic you used
- show where the metadata/mapping lives in the code
- list files changed
- note any interfaces that were ambiguous or required assumptions