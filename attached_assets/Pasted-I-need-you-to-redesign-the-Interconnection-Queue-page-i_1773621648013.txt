I need you to redesign the Interconnection Queue page in my React/TypeScript NYISO dashboard so it becomes a true market intelligence page instead of just a table viewer.

First inspect the codebase and identify the files responsible for:
- the Interconnection Queue page
- the queue scraping logic
- the table component
- KPI cards
- any chart components already used in the app
- shared layout components (cards, grids, sections)

Then implement the redesign described below.

Important rules:
- Do not use emojis or decorative icons anywhere in the UI.
- Maintain the clean dashboard design used across the rest of the application.
- Reuse existing chart and card components where possible.
- Avoid excessive whitespace.
- Make the page visually dense but still readable.

The goal is to transform the queue page into a market analysis dashboard.

--------------------------------------------------

SECTION 1 — Queue Intelligence Summary

At the top of the page create a short summary section titled:

"Queue Intelligence"

This should automatically generate a short analysis based on the queue data.

Example insights:
- total queue capacity
- dominant technology types
- zones with the most queued capacity
- recent additions to the queue
- expected buildout trends

This can be a deterministic summary or AI-generated if the existing app supports OpenAI summaries.

--------------------------------------------------

SECTION 2 — Replace Current KPIs With Market-Relevant KPIs

Instead of just counting projects, compute meaningful metrics.

Create KPI cards for:

Active Queue Capacity (MW)
Cluster Study Capacity (MW)
In-Service Capacity (MW)
Withdrawn Capacity (MW)

Storage Capacity (MW)
Solar Capacity (MW)
Wind Capacity (MW)

Largest Project (MW)

Average Project Size

Make sure these values are calculated from the queue dataset.

--------------------------------------------------

SECTION 3 — Recent Queue Activity Panel

Improve the current "Recent Changes" list.

Add a summary above it showing:

New Projects Added Since Last Update
Projects Withdrawn
MW Added Since Last Update

Then display the list of new projects.

Each entry should show:

Queue ID
Project Name
Developer
Fuel Type
MW
Zone

--------------------------------------------------

SECTION 4 — Queue Analytics Charts

Add two charts under the KPIs.

Chart 1: Queue Capacity by Fuel Type

Example fuels:
Solar
Wind
Storage
Gas
Hybrid
Other

Display total MW for each.

Recommended chart type:
horizontal bar chart.

Chart 2: Queue Capacity by Zone

Aggregate MW by NYISO zone (A–K).

Recommended chart type:
bar chart or stacked bar chart.

--------------------------------------------------

SECTION 5 — Queue Pipeline Visualization

Add a small pipeline visualization showing how projects progress through the queue.

Stages:

Cluster
Active
In Service

Display MW totals for each stage.

Use a horizontal pipeline or funnel chart.

--------------------------------------------------

SECTION 6 — Largest Projects Section

Add a small table showing the largest projects.

Columns:

Queue #
Project Name
Developer
Fuel Type
MW
Zone

Sort by MW descending.

Show top 15 projects.

--------------------------------------------------

SECTION 7 — Improve the Queue Table

Keep the full queue table but improve usability.

Add filters for:

Status
Fuel Type
Zone
MW Range

Add sorting by:

MW
Developer
Zone
Queue Number

Add search functionality for:

Project Name
Developer
County
Queue #

Highlight large projects (>300 MW).

Make the table collapsible so the page does not become extremely long.

--------------------------------------------------

SECTION 8 — Reduce Vertical Scrolling

Convert the page into collapsible sections:

Queue Intelligence
Queue KPIs
Queue Analytics
Recent Queue Changes
Largest Projects
Queue Table

Default expanded sections:

Queue Intelligence
Queue KPIs
Queue Analytics

Collapse the full queue table by default.

--------------------------------------------------

SECTION 9 — Scrape Button Behavior

Move the "Scrape Queue" button to the upper right of the page header.

Also show:

Last Updated Timestamp

Example:

Last Updated: 2026-03-15 09:42

--------------------------------------------------

SECTION 10 — Design Requirements

Ensure:

All KPI cards are evenly sized
Charts align with the rest of the dashboard design
Section headers match existing style
Spacing between sections is consistent
No emojis
No decorative icons
No oversized whitespace

--------------------------------------------------

SECTION 11 — Implementation

After implementing, show me:

1. Which files were modified
2. Which new files were created
3. The new chart components
4. The transformation logic for queue analytics
5. Any commands required to run the changes