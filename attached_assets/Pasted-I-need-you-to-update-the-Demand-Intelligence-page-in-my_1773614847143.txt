I need you to update the Demand Intelligence page in my React/TypeScript NYISO dashboard.

Important:
- Focus only on the Demand page and the components/hooks/utilities it depends on.
- Do not refactor unrelated pages unless required for shared controls or reusable chart components.
- First inspect the existing codebase and identify the exact files responsible for:
  - the Demand page
  - KPI cards
  - demand summary box
  - chart rendering
  - resolution controls
  - zone selectors
  - dataset fetching via useDataset/useInventory
- Then implement the edits below end-to-end.

GOALS

I want the Demand page to adopt the same product/UI direction as the revised Price page.

Main edits:
1. Move the Demand Summary box to the top of the page.
2. Enhance the Demand Summary using the app’s OpenAI integration if it already exists.
3. Change the KPI cards so they default to fixed “key market insight” demand metrics and do NOT depend on whichever chart resolution is selected.
4. Replace the top resolution bar with a side control panel for charting.
5. Add richer chart controls:
   - zone selection
   - resolution / aggregation selection
   - date range selection
   - chart type selection
6. Improve tooltip/hover behavior.
7. Rework the lower demand charts so they follow the same charting/control model and feel more unified.
8. Keep the UI clean, modern, and consistent with the current dashboard aesthetic.

DETAILS / REQUIRED BEHAVIOR

PART 1 — Move Demand Summary to the top
Move the “Demand Summary” box to the top of the page, above KPI cards and charts.

Enhance the summary:
- If the app already has OpenAI/API-based summary generation, reuse it.
- If no AI integration exists, create a clean fallback architecture but do not block the page.
- The summary must be grounded in computed demand statistics only.
- It should produce concise commentary on things like:
  - forecast vs actual relationship
  - peak system load
  - timing of peak
  - forecast error direction/magnitude
  - notable volatility or stress windows
- Include a deterministic fallback summary if AI is unavailable or fails.

Suggested helper structure:
- buildDemandSummaryContext(...)
- generateDemandSummary(...)

PART 2 — KPI cards should use fixed market insight metrics
Replace the current KPI logic.

The KPI cards should always show default demand intelligence metrics, regardless of selected chart resolution.

Required KPI metrics:
1. On-Peak Avg Forecast Load
2. On-Peak Avg Actual Load
3. Peak Forecast Load + hour/date
4. Peak Actual Load + hour/date
5. Low Forecast Load + hour/date
6. Low Actual Load + hour/date
7. Avg Forecast Error
8. Peak Forecast Error
9. Largest Under-Forecast interval
10. Largest Over-Forecast interval

Definitions:
- Forecast Error = Forecast - Actual unless the codebase already defines it differently
- On-Peak should use HE 7–22 unless the project already uses a different standard
- Over-forecast means forecast > actual
- Under-forecast means forecast < actual

Important:
- These KPIs should be computed from the base source datasets, not from whatever transformed chart view is currently selected.
- Do not tie KPI values to the chart resolution selector.
- Use the NYISO total where appropriate, but keep zone-aware logic where the current datasets support it.

UI guidance:
- Keep KPI styling aligned with current dashboard cards
- If needed, expand to a two-row responsive grid
- Prioritize readability over squeezing everything into one row

PART 3 — Replace the top resolution bar with side “Chart Controls”
The current resolution bar above the page should be removed and replaced with a side control panel associated with the main chart section.

Create a reusable controls component, for example:
- DemandChartControls.tsx

The control panel should include:
1. Zone selector
   - multi-select
   - “All Zones” shortcut
   - default to all zones
2. Resolution / aggregation selector
   - Hourly
   - On-Peak Average
   - Off-Peak Average
   - Daily Average
3. Date range selector
   - Today (default)
   - Custom start/end date
   - All Dates
4. Chart type selector
   - line with markers
   - regular line chart
   - stacked area chart
   - stacked bar chart
5. Optional useful toggles if appropriate:
   - show legend
   - smooth lines on/off
   - compare forecast vs actual on/off
   - normalize off by default

Layout preference:
- desktop: controls on the side of the main chart card
- smaller screens: controls collapse above or below the chart
- keep the implementation polished and consistent with the rest of the app

PART 4 — Main demand chart behavior
Update the main “System Load Forecast by Zone” chart so it supports the new controls.

Required behavior:
- user can select one or more zones
- user can choose resolution:
  - Hourly
  - OPA
  - OFFPA
  - Daily average
- user can choose date range:
  - today by default
  - custom date range
  - all dates
- user can switch chart type between:
  - line with markers
  - line
  - stacked area
  - stacked bar

Important:
- Make sure transformations are correct for each aggregation mode
- If stacked views are only sensible in some cases, handle that cleanly
- If a chart type is misleading for a selected configuration, disable it gracefully with a small explanatory note

Tooltip / hover behavior:
- hovering a timestamp should display all visible series values clearly at that timestamp
- tooltip should show:
  - timestamp/date
  - zone name
  - value
- if forecast and actual are both shown, clearly label which is which
- markers mode should make hovered points easy to see

PART 5 — Rework the lower charts so the experience is unified
The lower charts currently include:
- Forecast vs Actual (NYISO Total)
- Forecast Error

I want these to feel like part of the same charting system as the top chart.

Preferred direction:
- Rebuild these using shared charting primitives and shared control logic where possible
- Keep a unified visual language and interaction pattern

Acceptable implementation patterns:
A. Keep separate chart cards but share the same control state model
B. Add a chart mode toggle that lets the user switch among:
   - zonal load chart
   - NYISO total forecast vs actual
   - forecast error
C. Keep multiple chart cards but ensure consistent chart types, tooltips, legends, and date/resolution behavior

For the Forecast vs Actual chart:
- support date range selection
- support resolution selection where it makes sense
- tooltip should show both series at hovered timestamp

For the Forecast Error chart:
- support date range selection
- support resolution selection where it makes sense
- tooltip should show the error value clearly at hovered timestamp
- make positive vs negative error visually interpretable if feasible

PART 6 — Data transformation requirements
Please inspect the current demand dataset structure and create clean reusable transformation utilities.

I want reusable helpers for:
- filtering by date range
- filtering by zones
- computing OPA
- computing OFFPA
- computing daily averages
- aligning forecast vs actual by timestamp
- computing forecast error
- computing KPI stats
- computing peak/low timing metrics
- building AI summary context

Suggested utility files if needed:
- demandTransforms.ts
- demandMetrics.ts
- demandSummary.ts
- demandChartOptions.ts

Be careful with:
- timestamp parsing
- zone naming consistency
- missing/null values
- forecast vs actual alignment
- timezone assumptions
- HE logic for OPA/OFFPA
- total NYISO vs zonal rows

PART 7 — Demand Summary logic
The summary should be smarter and more useful.

At minimum it should discuss:
- peak forecast load
- peak actual load
- whether the system was over-forecast or under-forecast on average
- magnitude of average forecast error
- peak stress interval or largest surprise interval
- broad shape/trend over the selected/default period

If OpenAI integration exists:
- reuse it
- send structured computed stats, not giant raw payloads
- keep outputs concise and grounded

If no OpenAI integration exists:
- create a deterministic templated summary generator

PART 8 — Design / UX expectations
Keep the page visually close to the existing dashboard style:
- clean card layout
- rounded corners
- soft borders
- readable typography
- modern analytics feel

Desired hierarchy:
1. Demand Summary at the top
2. KPI cards below summary
3. Main chart section with side Chart Controls
4. Lower charts rebuilt into a unified charting system
5. Detailed Data section remains below

Avoid clutter.
Do not make the page feel like a raw developer dashboard.

PART 9 — Shared-component reuse
If the Price page already has or will have reusable chart controls/components after recent edits, reuse them where sensible rather than duplicating logic.

For example:
- shared side control panel
- shared date range picker
- shared multi-zone selector
- shared chart wrapper/card component
- shared tooltip formatter

But do not over-refactor unrelated modules unless needed.

PART 10 — Deliverables I want from you
After implementing, show me:
1. Which files you changed
2. Which new files you added
3. The key code snippets
4. Any commands I need to run
5. Any assumptions you made

Please do not stop to ask me follow-up questions unless absolutely necessary.
Make reasonable assumptions from the current codebase and implement the cleanest working version.