11. Add a generator geographic price map
I also want you to add a geographic New York generator price map feature inspired by the NYISO-style zonal map screenshot I attached, but built in a clean modern product style for GridScopeNY.

Goal:
Create a map-based visualization of generator-level prices in New York using generator latitude/longitude coordinates.

Data sources to use:
- generator_names (contains generator metadata including latitude / longitude if available)
- da_gen_lmp
- rt_gen_lmp

Map behavior:
- Plot generator points geographically across New York State using latitude and longitude from generator_names
- Join generator metadata to:
  - day-ahead generator LMP data from da_gen_lmp
  - real-time generator LMP data from rt_gen_lmp

For each plotted generator point, support displaying:
- LMP
- MLC
- MCC
- market type:
  - DA
  - RT

User controls:
- market toggle: Day Ahead / Real Time
- metric toggle: LMP / MLC / MCC
- date selector
- hour selector if available
- optional generator / zone / fuel-type filter if the metadata supports it
- optional color scale selector if needed

Visual requirements:
- Use a real geographic New York map or clean simplified geographic background
- This should be a point-based generator map, not only a zonal chart
- Color each generator point by the selected metric value
- Use a clear color scale / legend
- Add hover tooltips that show:
  - generator name
  - PTID if available
  - zone
  - selected metric value
  - DA or RT
  - timestamp / HE
  - optional fuel type if available

Map UX:
- If there are many points, use sensible marker sizing, transparency, or clustering only if needed
- Do not silently drop generator points
- If some generators are missing coordinates, report how many could not be mapped
- Show counts such as:
  - total generators in dataset
  - mapped generators
  - unmapped generators

Join/data audit requirements:
- Verify the join keys between generator_names and da_gen_lmp / rt_gen_lmp
- Do not assume the joins are already correct
- Explicitly audit for:
  - missing coordinates
  - unmatched PTIDs / generator IDs
  - duplicate mappings
  - rows dropped during merge
- Tell me clearly how many generator records successfully map to coordinates

Placement in app:
Choose the cleanest product placement.
Preferred options:
- either add this as a new section inside the Prices page
- or add a dedicated “Generator Map” section/page under Market Data
Use your judgment based on current app structure, but keep it integrated with the product.

Design direction:
- Use the attached ISO screenshot only as inspiration for the concept
- Do not copy the exact visual design literally
- Keep it modern and consistent with GridScopeNY
- Use clean cards, filters, and legends
- Make it feel like a real market intelligence feature

Performance:
- If needed, preprocess the mapped generator dataset for faster rendering
- If processed CSVs are currently used, prefer Parquet for the mapped generator outputs as well
- Keep the map responsive enough for demo use

Deliverables for this feature:
1. implement the generator geographic price map
2. explain what files were added/changed
3. explain how the generator metadata was joined to DA/RT generator LMP files
4. report mapping coverage:
   - total generators
   - matched generators
   - unmatched generators
5. explain any assumptions about coordinates or identifiers

Acceptance criteria for this feature:
- I can switch between DA and RT generator prices
- I can switch between LMP, MLC, and MCC
- Generator points are plotted geographically across New York
- Hovering a point gives useful generator-level information
- The map is based on real joined data, not mock points
- Missing/unmapped generators are accounted for explicitly