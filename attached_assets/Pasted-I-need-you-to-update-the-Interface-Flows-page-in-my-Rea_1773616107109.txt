I need you to update the Interface Flows page in my React/TypeScript NYISO dashboard.

Important:
- Focus only on the Interface Flows page and the components/hooks/utilities it depends on.
- Do not refactor unrelated pages unless required for shared controls or reusable chart components.
- Leave the TTCF Derates table section unchanged. That table already looks good to me.
- You can improve spacing/hierarchy around TTCF if needed, but do not redesign or materially change the TTCF table itself.
- First inspect the codebase and identify the exact files responsible for:
  - the Interface Flows page
  - KPI cards
  - flow summary box
  - chart rendering
  - resolution controls
  - class/interface selectors
  - dataset fetching via useDataset/useInventory
  - TTCF Derates section
- Then implement the edits below end-to-end.

GOALS

I want the Interface Flows page to follow the same UI/product direction as the revised Price, Demand, and Generation pages.

Main edits:
1. Move the Flow Summary box to the top of the page.
2. Enhance the Flow Summary using the app’s OpenAI integration if it already exists.
3. Change the KPI cards so they use fixed “key interface insights” and do NOT depend on the currently selected chart resolution.
4. Replace the top resolution/control bar with a side chart control panel.
5. Add richer chart controls:
   - interface selection
   - class selection
   - resolution / aggregation selection
   - date range selection
   - chart type selection
6. Improve tooltip/hover behavior.
7. Rework the main flow chart and interface summary section so the experience is more unified.
8. Leave the TTCF Derates table unchanged.
9. Keep the UI clean, modern, and consistent with the current dashboard aesthetic.

DETAILS / REQUIRED BEHAVIOR

PART 1 — Move Flow Summary to the top
Move the “Flow Summary” box to the top of the page, above KPI cards and charts.

Enhance the summary:
- If the app already has OpenAI/API-based summary generation, reuse it.
- If no AI integration exists, create a clean fallback architecture but do not block the page.
- The summary must be grounded in computed interface statistics only.
- It should produce concise commentary on things like:
  - most active interface
  - strongest internal path
  - strongest external path
  - highest absolute flow
  - import/export pressure
  - whether flows were concentrated or broad-based
  - notable negative-flow interfaces if present
- Include a deterministic fallback summary if AI is unavailable or fails.

Suggested helper structure:
- buildFlowSummaryContext(...)
- generateFlowSummary(...)

PART 2 — KPI cards should use fixed interface insight metrics
Replace the current KPI logic.

The KPI cards should always show default flow intelligence metrics, regardless of selected chart resolution.

Required KPI metrics:
1. On-Peak Avg Total Internal Flow
2. On-Peak Avg Total External Flow
3. Peak Positive Flow + interface + hour/date
4. Peak Negative Flow + interface + hour/date
5. Most Active Interface
6. Top Internal Interface
7. Top External Interface
8. Active Interfaces Count

Definitions / guidance:
- On-Peak should use HE 7–22 unless the project already uses a different standard
- “Most Active Interface” should be based on the best defensible activity measure from the dataset, likely highest average absolute flow or highest total absolute movement over the default analysis window
- Peak Positive Flow = maximum positive MW observed
- Peak Negative Flow = minimum / most negative MW observed
- Active Interfaces Count should reflect the interfaces present with meaningful observations in the default analysis window

Important:
- These KPI values must come from the base source datasets, not from whatever transformed chart view is currently selected
- Do not tie KPI card values to the chart resolution selector
- If class labels already exist as Internal / External, use them
- Preserve any existing direction semantics already used in the app

UI guidance:
- Keep KPI styling aligned with current dashboard cards
- If needed, expand to a two-row responsive grid
- Prioritize readability over squeezing everything into one row

PART 3 — Replace the top control bar with side “Chart Controls”
The current resolution/class/interface control bar above the charts should be removed and replaced with a side control panel associated with the main chart section.

Create a reusable controls component, for example:
- InterfaceChartControls.tsx

The control panel should include:
1. Interface selector
   - multi-select
   - “All Interfaces” shortcut
   - default to all interfaces or a sensible visible subset if the chart becomes too crowded
2. Class selector
   - All
   - Internal
   - External
3. Resolution / aggregation selector
   - Hourly
   - On-Peak Average
   - Off-Peak Average
   - Daily Average
4. Date range selector
   - Today (default)
   - Custom start/end date
   - All Dates
5. Chart type selector
   - line with markers
   - regular line chart
   - stacked area chart
   - stacked bar chart
6. Optional useful toggles if appropriate:
   - show legend
   - smooth lines on/off
   - show absolute values
   - show top N only
   - normalize off by default

Layout preference:
- desktop: controls on the side of the main chart card
- smaller screens: controls collapse above or below the chart
- keep the implementation polished and consistent with the rest of the app

PART 4 — Main interface flow chart behavior
Update the main “Interface Flows Over Time” chart so it supports the new controls.

Required behavior:
- user can select one or more interfaces
- user can filter by class:
  - All
  - Internal
  - External
- user can choose resolution:
  - Hourly
  - OPA
  - OFFPA
  - Daily average
- user can choose date range:
  - today by default
  - custom date range
  - all dates
- user can switch chart type between:
  - line with markers
  - line
  - stacked area
  - stacked bar

Important:
- Make sure transformations are correct for each aggregation mode
- Flow sign must be preserved correctly for charts where sign matters
- If stacked charts would be misleading when positive and negative flows are mixed, handle that cleanly:
  - either implement a safe diverging approach
  - or disable stacked mode for incompatible configurations with a small explanatory note
- Do not distort directional interpretation

Tooltip / hover behavior:
- hovering a timestamp should display all visible interface values clearly at that timestamp
- tooltip should show:
  - timestamp/date
  - interface name
  - value in MW
  - class if helpful
- preserve positive/negative sign clearly
- markers mode should make hovered points easy to see

PART 5 — Rework the interface summary section so the page feels unified
The current page includes:
- main flow chart
- interface summary table
- flow summary box
- TTCF Derates section

I want the non-TTCF part of the page to feel like a unified analytics workspace.

Preferred direction:
- Rebuild the main chart and interface summary section using shared charting/layout primitives where possible
- Keep a unified visual language and interaction pattern

For the Interface Summary table:
- keep it, but make sure it is derived from the current/default analysis window where appropriate
- preserve the useful columns, such as:
  - interface
  - class
  - region/path
  - direction
  - avg flow
  - max flow
  - min flow
  - observations
- sort by a meaningful default, such as average absolute flow descending
- make sure class badges and numbers are styled consistently

Optional improvement if easy:
- allow clicking a row to focus/highlight that interface in the chart
- allow table rows to reflect current class filter

PART 6 — Data transformation requirements
Please inspect the current interface-flow dataset structure and create clean reusable transformation utilities.

I want reusable helpers for:
- filtering by date range
- filtering by interface selection
- filtering by class
- computing OPA
- computing OFFPA
- computing daily averages
- computing total internal flow
- computing total external flow
- computing absolute activity metrics
- computing KPI stats
- computing peak/low timing metrics
- building AI summary context

Suggested utility files if needed:
- interfaceTransforms.ts
- interfaceMetrics.ts
- interfaceSummary.ts
- interfaceChartOptions.ts

Be careful with:
- timestamp parsing
- interface naming consistency
- class mapping consistency
- missing/null values
- positive vs negative flow semantics
- timezone assumptions
- HE logic for OPA/OFFPA
- whether totals should be signed or absolute depending on the metric

PART 7 — Flow Summary logic
The summary should be smarter and more useful.

At minimum it should discuss:
- most active interface
- strongest internal interface
- strongest external interface
- max positive flow
- max negative flow
- whether flows suggest concentrated transfer pressure or broader system movement
- how many internal vs external interfaces were active

If OpenAI integration exists:
- reuse it
- send structured computed stats, not giant raw payloads
- keep outputs concise and grounded

If no OpenAI integration exists:
- create a deterministic templated summary generator

PART 8 — TTCF Derates section must remain intact
Leave the TTCF Derates table unchanged.

Specifically:
- do not redesign the TTCF table
- do not alter its columns or detailed row rendering
- do not change its data logic unless absolutely necessary for bug fixes
- do not replace it with a different chart/table experience

Allowed minor changes:
- spacing above/below the TTCF section
- heading hierarchy so it visually fits the page better
- card wrapper consistency if needed, as long as the TTCF table itself stays the same

PART 9 — Design / UX expectations
Keep the page visually close to the existing dashboard style:
- clean card layout
- rounded corners
- soft borders
- readable typography
- modern analytics feel

Desired hierarchy:
1. Flow Summary at the top
2. KPI cards below summary
3. Main chart section with side Chart Controls
4. Interface Summary section below
5. TTCF Derates section below that, with its table unchanged
6. Detailed datasets section remains below if it already exists

Avoid clutter.
Do not make the page feel like a raw developer dashboard.

PART 10 — Shared-component reuse
If the Price, Demand, and Generation pages already have or will have reusable chart controls/components after recent edits, reuse them where sensible rather than duplicating logic.

For example:
- shared side control panel
- shared date range picker
- shared multi-select control
- shared chart wrapper/card component
- shared tooltip formatter
- shared summary card pattern

But do not over-refactor unrelated modules unless needed.

PART 11 — Deliverables I want from you
After implementing, show me:
1. Which files you changed
2. Which new files you added
3. The key code snippets
4. Any commands I need to run
5. Any assumptions you made

Please do not stop to ask me follow-up questions unless absolutely necessary.
Make reasonable assumptions from the current codebase and implement the cleanest working version.